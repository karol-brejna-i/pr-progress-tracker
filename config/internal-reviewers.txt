# INTERNAL reviewers — our own teammates, who review first, before the PR is handed to the
# upstream repo maintainers (see external-reviewers.txt).
#
# An APPROVED review by a login here satisfies the "internally approved" milestone. The
# earliest such approval counts; there is no N-approvals threshold.
#
# One login per line; case-insensitive; a leading @ is stripped.
# A login must not appear in both this file and external-reviewers.txt (that fails the run).

liangan1
